[package]
name = "tcp_channel"
version = "0.1.0"
edition = "2018"

[dependencies]
libc = "0.2"
observe = {path = "../observe"}
serde = "1.0"
serde_json = "1.0"

[lib]
name = "tcp_channel"
path = "lib.rs"
